"use client";

import { usePortal } from "@/components/portal-context";
import { PageHeader, StatusBadge } from "@/components/portal-ui";

export default function DesignersPage() { const { orders } = usePortal(); const names = ["UBAID", "HASEEB", "IBRAHIM", "JUTT", "TUFAIL", "ZESHAN", "SHAHBAZ JR"]; return <><PageHeader eyebrow="Team capacity" title="Designer performance" description="A clear view of today's assignments and throughput." /><section className="panel"><div className="panel-heading"><div><p className="eyebrow">Today</p><h2>Production team</h2></div><span className="team-count">{names.length} designers</span></div><div className="designer-list">{names.map((name) => { const assigned = orders.filter((order) => order.designer === name); const completed = assigned.filter((order) => order.status === "Completed").length; return <div className="designer-row" key={name}><div className="avatar avatar-large">{name.slice(0, 1)}</div><div className="designer-name"><strong>{name}</strong><span>{assigned.length ? `${assigned.length} assigned today` : "Available for assignment"}</span></div><div className="progress-wrap"><div className="progress"><span style={{ width: `${assigned.length ? Math.max(15, (completed / assigned.length) * 100) : 0}%` }} /></div><small>{completed}/{assigned.length} complete</small></div><StatusBadge status={assigned.length ? "In Design" : "Available"} /></div>; })}</div></section></>; }